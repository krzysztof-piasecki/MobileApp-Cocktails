<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ikona_aplikacji"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ikona_aplikacji"
        android:supportsRtl="true"
        android:theme="@style/MotywAplikacji"
        tools:targetApi="31">
        <activity android:name=".ActionActivity"
            android:label="@string/action_title"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".DetailsActivity"
            android:exported="false"
            android:theme="@style/Theme.Cocktails" />
        <activity
            android:name=".CocktailDetailActivity"
            android:parentActivityName=".MainActivity">
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="pl.put.poznan.aplikacje.mobilne.cocktails.SingleFragmentActivity" />
    </application>

</manifest>